import 'package:flutter_test/flutter_test.dart';
import 'package:pure_drop_aqua/main.dart';

void main() {
  testWidgets('App initializes test', (WidgetTester tester) async {
    expect(PureDropAquaApp, isNotNull);
  });
}
